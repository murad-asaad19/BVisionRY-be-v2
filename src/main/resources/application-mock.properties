# The `mock` profile is now a thin alias for the transport switch it always
# meant, so `SPRING_PROFILES_ACTIVE=dev,mock` (docker-compose) and every doc and
# script that names it keep working unchanged — and adding `mock` to ANY profile
# still forces the static AI transport, which is the one thing it is for.
bvisionry.ai.mock.enabled=true
