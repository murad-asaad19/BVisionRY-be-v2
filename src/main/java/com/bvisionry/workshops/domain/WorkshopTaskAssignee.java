package com.bvisionry.workshops.domain;

public enum WorkshopTaskAssignee {
    /** Performed once per team, by the team lead. */
    LEAD,
    /** Performed individually by every non-lead team member, after sharing. */
    MEMBER
}
